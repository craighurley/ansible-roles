---
- hosts: localhost
  remote_user: root
  roles:
    # ssh depends on users, users depends on bash
    - locale
    - sudo
    - yum-update
    - yum
    - harden
    - ntp
    - docker-znc
    - znc
